{
    "name": "ignis/swoole",
    "description": "A shim for a subset of the Swoole coroutine API on top of Ignis fibers (E15d). It declares global Swoole\\* names, so it is deliberately not autoloaded: point auto_prepend_file at vendor/ignis/swoole/src/shim.php. A compatibility layer, not a Swoole implementation.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.4",
        "ignis/runtime": "@dev"
    },
    "config": {
        "allow-plugins": false
    }
}
